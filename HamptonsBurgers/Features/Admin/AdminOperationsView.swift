import SwiftUI

/// Day-to-day admin tools for staff: live store status.
struct AdminOperationsView: View {
    var body: some View {
        NavigationStack {
            AdminStoreStatusView()
        }
    }
}
